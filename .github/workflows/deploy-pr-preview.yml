name: Deploy pr preview

on:
  pull_request:
    types:
      - opened
      - synchronize
      - closed
    paths:
      - "docs/sources/**"

permissions: {}

jobs:
  deploy-pr-preview:
    permissions:
      contents: read
      id-token: write
      pull-requests: write
      statuses: write
    if: "!github.event.pull_request.head.repo.fork"
    uses: grafana/writers-toolkit/.github/workflows/deploy-preview.yml@main # zizmor: ignore[unpinned-uses]
    with:
      branch: ${{ github.head_ref }}
      event_number: ${{ github.event.number }}
      repo: mcp-doc-server
      sha: ${{ github.event.pull_request.head.sha }}
      sources: |
        [
          {
            "index_file": null,
            "relative_prefix": "/docs/mcp-doc-server/next/",
            "repo": "mcp-doc-server",
            "source_directory": "docs/sources",
            "website_directory": "content/docs/mcp-doc-server/next"
          }
        ]
      title: ${{ github.event.pull_request.title }}
